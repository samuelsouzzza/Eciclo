import React from 'react';

const styles = {
  height: '1px',
  borderRadius: '5px',
  margin: '5% 0 5% 0',
  backgroundColor: 'rgba(0, 0, 0, 0.09)',
  width: '100%',
};

export const Separate = () => {
  return <div style={styles} />;
};
